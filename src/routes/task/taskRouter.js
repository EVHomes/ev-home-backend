import { Router } from "express";
const taskRouter = Router();

// taskRouter.get("/team-sections", getTeamSections);
// taskRouter.post("/requirement-add", addRequirement);

export default taskRouter;
